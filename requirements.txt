# Data ingestion
pandas>=2.2.0
openpyxl>=3.1.0
python-calamine>=0.1.7    # fast Rust-backed XLSX reader (fallback for large files)
pyarrow>=14.0.0

# Delta Lake (pure Python, no Spark/JVM)
deltalake>=1.0.0

# SQL queries over Delta tables
duckdb>=1.1.0

# Delta Sharing server
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
pyyaml>=6.0
boto3>=1.34.0

# MCP server
mcp>=1.0.0

# Dev / testing
httpx>=0.25.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
